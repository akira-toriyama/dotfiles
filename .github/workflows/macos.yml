on:
  push:
  schedule:
    - cron: "* 10 * * *"

jobs:
  build:
    runs-on: macos-11

    steps:
      - uses: actions/checkout@v3

      - name: setup
        run: |
          ./script/setup.sh
        env:
          DOT_FILE_ROOT_PATH: /Users/runner/work/dotfiles
