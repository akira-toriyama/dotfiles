# .maint-ignore — 月次 Claude ドキュメント保守 (claude-maint) の除外 / pin リスト。
# ここに挙げた doc は「使用回数が低くても絶対に archive しない」。
# 1 行 1 パターン。# 始まりと空行は無視。
#
# 補足: plugin 由来の skill は symlink なので、集計・候補列挙の時点で自動的に
# 除外される（ここに書く必要はない）。このファイルは「自作だが常に残したい物」を
# pin する用途。内容の更新（リンク修正等）は pin していても行われる。

# 索引本体（常時ロードされる横断 house 挙動の核。archive は絶対しない）
CLAUDE.md

# 索引メンテ用コマンド（使用頻度は低いが索引運用の要）
commands/my-add-trigger.md
